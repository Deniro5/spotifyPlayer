import styled from "styled-components";

const Placeholder = () => {
  return <Text>Enter a term in the search bar to find new music!</Text>;
};

const Text = styled.p`
  margin: 20px auto;
  font-weight: 500;
  text-align: center;
  line-height: 30px;
`;

export default Placeholder;
